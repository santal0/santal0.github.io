# 编译原理第二章：词法分析

## 2.0 总览

### 2.0.1 词法分析的位置




词法分析不是孤立的，它属于整个编译前端的一环。

- 编译过程（Compiling Process）为了把一种语言翻译成另一种语言
- 编译器先要**拆开并理解**程序，再重新组织输出
- 前端做 **analysis（分析）**，
  1. **Lexical analysis**：把输入分成 token
     - 词法分析只负责“切词”
     - **不负责**理解完整语法
     - 更不负责程序语义
  2. **Syntax analysis**：分析语法结构
  3. **Semantic analysis**：分析程序意义
- IR（中间表示）把前端和后端隔开
- 后端做 **synthesis（综合）**



这一章的核心问题其实只有一个：

> **如何把“字符流”变成“单词流（token 流）”？**

也就是：源码明明只是一个个字符，编译器为什么能知道：

- `if` 是关键字
- `match0` 是标识符
- `0.0` 是实数
- `(` 是左括号
- 空格和注释该忽略

这一章的路线是：

1. 先理解 **token 是什么**
2. 再用 **正则表达式（RE）** 描述 token
3. 再用 **有限自动机（FA）** 实现这些规则
4. 然后引入 **NFA**
5. 再把 NFA 变成 **DFA**
6. 再把 DFA **最小化**
7. 最后说明为什么现实里常用 **Lex/flex** 自动生成词法分析器


**自然语言描述 → 正则表达式 → 自动机 → 词法分析器实现**。



### 2.0.2. 词法分析器的任务：

- 输入：**字符流**
- 输出：**token 流**
- 会识别：
  - 名字（identifier）
  - 关键字（keyword）
  - 标点符号（punctuation）
- 会丢弃：
  - 空白符
  - 注释

课件给了一个例子：

```c
 float match0(char *s) /* find a zero */
 { 
     if (!strncmp(s, "0.0", 3))
 	return 0.; 
} 
```

这段 C 程序会被拆成

```
FLOAT  ID(match0)  LPAREN  CHAR STAR ID(s) RPAREN 
LBRACE  
    IF LPAREN BANG ID(strncmp) LPAREN ID(s) COMMA STRING(0.0) COMMA NUM(3) RPAREN  RPAREN
    RETURN REAL(0.0) SEMI 
RBRACE
EOF 
```

### 2.0.3. 接口

词法分析器的接口通常像一个函数，例如：

- `getToken()`
- 或 `yylex()`

每调用一次，就返回下一个 token。

为什么要用形式化工具来研究词法分析：

- 因为后面语法分析也会用类似形式化方法
- 而且这些工具不只在编译里有用，例如日志解析



## 2.1 Lexical Token

### 2.1.1. 定义 lexical token

- token 是一段字符序列
- 它是编程语言语法中的一个单位
- token 类型是有限集合

例子：

|  Token   |            例子            |
| :------: | :------------------------: |
|   `ID`   |        foo n14 last        |
|  `NUM`   |      73 0 00 515 082       |
|  `REAL`  | 66.1 .5 10.  1e67  5.5e-10 |
|   `IF`   |             if             |
| `COMMA`  |             ,              |
| `NOTEQ`  |             !=             |
| `LPAREN` |             (              |
| `RPAREN` |             )              |



### 2.1.2. 词法分析器的输出

#### 2.1.2.1. 输出包含什么

这里用具体程序演示 token 化结果，还是这个例子：

```c
 float match0(char *s) /* find a zero */
 { 
     if (!strncmp(s, "0.0", 3))
 	return 0.; 
} 
```

这段 C 程序会被拆成

```
FLOAT  ID(match0)  LPAREN  CHAR STAR ID(s) RPAREN 
LBRACE  
    IF LPAREN BANG ID(strncmp) LPAREN ID(s) COMMA STRING(0.0) COMMA NUM(3) RPAREN  RPAREN
    RETURN REAL(0.0) SEMI 
RBRACE
EOF 
```

这里可见：

- 词法分析器不仅返回 token 的**类型**
- 有些 token 还带**语义值（semantic value）**

例如：

- `ID(match0)`：类型是 ID，值是 `match0`
- `NUM(3)`：类型是 NUM，值是 `3`
- `REAL(0.0)`：类型是 REAL，值是 `0.0`

也就是说：

> token 不只是标签，还可能带“附加信息”。

#### 2.1.2.2. 输出不包含什么？



- **源码里有的东西，不一定都变成 token**
- 一部分内容会被直接忽略或提前处理掉。





- 预处理器会先处理某些内容
- 词法分析器面对的是预处理后的字符流





1. 保留字不能当标识符，比如`IF`、`VOID`、`RETURN`

2. 什么东西不是 token：注释、预处理指令、宏、空格、tab、换行



## 2.2. 正则表达式

### 2.2.0. 词法分析器的实现&为什么需要正则表达式

> 已经知道输入和输出了，那词法分析器怎么实现？

答案是：先要描述语言的词法规则。

#### 2.2.0.1. 如何描述词法规则

这里用 C/Java 的标识符规则举例，说明“自然语言版词法规则”：

1. 标识符由字母和数字组成
2. 第一个字符必须是字母
3. `_` 算字母
4. 大小写不同
5. 下一个 token 要取**尽可能长的字符串**
   * **最长匹配（longest match）**：能多吃就多吃。
6. 空白和注释一般忽略，但有时空白用于分隔相邻 token

#### 2.2.0.2. 如何实现词法分析器

1. **ad hoc lexer**：手写任何编程语言来实现
   * 这样十分繁琐
2. 更简单的方法
   * **regular expressions**：用正则描述 token
     * 易懂，但直接实现麻烦
   * **deterministic finite automata**：用 DFA 实现 lexer
     * 容易实现，但手工从规则构造不方便
   * 数学把两者连接起来



#### 2.2.0.3. “从规则到程序”的总路线

1. 自然语言描述 lexical tokens

2. 将自然语言转成 regular expression

3. regular expression 转成 deterministic finite automata

4. 最后按照 DFA 实现 lexer



## 2.2 Regular Expression

### 2.2.1 基础概念：

- **language**：字符串集合
- **string**：符号的有限序列
- **symbol**：来自有限字母表的元素





- 这里先**不讨论字符串意义**
- 只讨论“它属不属于这个集合”

比如：

- C 语言关键字集合是一个语言
- 所有合法标识符也是一个语言



定义正则表达式的作用：

- 正则表达式可以用有限描述表示某些可能无限的语言
- 每个正则表达式 `r` 表示一个正规语言 `L(r)`

### 2.2.2. 正则表达式的定义

#### 2.2.2.1. 定义

定义两种最基本的正则：

* **普通符号 `a`**
  * 表示只含字符串 `"a"` 的语言

* **ε**
  * 表示只含空串 `""` 的语言

定义两个组合操作：

* **Alternation（并/选择）**：`M | N`
  * 属于 `L(M)` 或 `L(N)` 的字符串都算
  * `a|b` 表示 `{a, b}`

* **Concatenation（连接）**：`M · N`
  * 先来自 `M`，后来自 `N`
  * `(a|b)·a` 表示 `{aa, ba}`

定义 **Kleene closure（Kleene 星号）**：

- `M*` 表示 `M` 中字符串重复 0 次或多次

  - `((a|b)·a)*`可以表示 `{"", "aa",  "ba", "aaaa", "baaa", "aaba", "baba", "aaaaaa", …}`

  它可以表示无限多个字符串，因为重复次数不受限。

例题：

- `(0|1)*·0` 表示**二进制中所有能被 2 整除的数**
- `b*(abb*)*(a|ε)`：由 `a` 和 `b` 构成且没有连续两个 `a`

- `(a|b)*aa(a|b)*`：所有包含连续 `aa` 的由 `a,b` 构成的字符串

`.`：任意单个字符（通常不含换行）

`"<一个字符串>"`：引用，表示字符串自身

#### 2.2.2.2. 规则

讲正则简写规则和优先级：

- `*` 绑定最紧
- 连接比 `|` 更紧

  - `ab|c` 其实是 `(a·b)|c`
  - `(a|)` 其实是 `(a|ε)`



#### 2.2.2.3. 缩写

缩写**不增加表达能力**

- `[abcd]` 等价于 `(a|b|c|d)`
- `[b-g]` 是区间等价于 `[bcdefg]`
- `[b-gM-Qkr]` 等价于 `[bcdefgMNOPQkr]`
- `M?` 等价于 `(M|ε)`
- `M+` 等价于 `M·M*`





### 2.2.3. 用正则描述 token

#### 2.2.3.1. 示例

|               正则表达式               |       行为        |
| :------------------------------------: | :---------------: |
|                遇到`if`                | 做` {return IF;}` |
|          遇到`[a-z][a-z0-9]*`          | 做` {return ID;}` |
|                `[0-9]+                 |  {return NUM;}`   |
| ` ([0-9]+"."[0-9]*)|([0-9]*"."[0-9]+)` |     实数规则      |
|            注释和空白的规则            |  匹配后“不做事”   |
|                 非法字符                 |   报错    |

#### 2.2.3.2. 词法规则必须**完整**

* 就是任何输入字符都应该被某条规则处理到。
* 所以最后要放一个“兜底规则”：
  * 任何单字符都能匹配到
  * 如果不是合法字符，就报错

#### 2.2.3.3. 正则规则会有歧义：

- `if8` 应该识别为 `IF` + `8`，还是一个 `ID`？
- `if 89` 开头的 `if` 应该识别成保留字还是标识符？

##### 2.2.3.3.1. 消歧规则：

1. Longest match（最长匹配）
   * 取能匹配任一正则的**最长前缀**。

2.  Rule priority（规则优先级）
   * 如果同一个最长前缀可被多条规则匹配，则取**最先写的规则**。

于是：

- `if8` 按最长匹配，应识别为 `ID`
- `if` 本身同时符合 `IF` 和 `ID`，按规则顺序，识别为 `IF`



## 2.3. Finite Automata

>正则适合描述 token，但真正计算机程序实现时，更适合用有限自动机

### 2.3.1. 有限自动机形式定义：

一个自动机包含：

- 有限的状态集合 `S`
- 字母表 `Σ`
- 转移函数 `move`
- 初始状态 `s0`
- 终态集合 `F`



自动机图的画法：

- 圆圈表示状态
- 双圆表示终态
- 从外面指向某状态的箭头表示初态
- 一条边标多个字符是多条平行边的简写

### 2.3.1. DFA（确定有限自动机）：

#### 2.3.1.1. 定义

- 同一状态出发，不能有两条相同字符标记的边

 DFA 如何接受字符串：

1. 从初态出发
2. 对每个输入字符走唯一一条边
3. 所有字符读完后若停在终态，则接受
4. 否则拒绝

> 每一步都没有歧义，只有一个下一步。



#### 2.3.1.2. 合并

> 现在有 6 个分开的自动机，怎么合成一个真正的词法分析器？

> 真正 lexer 不能每种 token 单独一台机器，而要合成成一台总机器。

每个终态标记它识别的 token 类型



解释组合后的状态 `I`：

- 它同时像 IF 自动机里的`I`
- 也像 ID 自动机里的`I`
- 因为后者是终态，所以合并后也必须是终态

这说明合并自动机时，一个状态可能同时继承多个“角色”。


#### 2.3.1.3. 如何把自动机编码成程序：

- 用一个**转移矩阵** `trans[state][char]`，表示在 `state` 状态的时候遇到字符 `char` 的时候应该如何转移。
- 没有边时用 **dead state（死状态）0**

  - 死状态对所有字符都回到自己

- 再用一个 **finality array** 记录哪些**终止**状态对应什么动作



#### 2.3.1.4. 自动机运行

维护两个变量：

- `Last-Final`
- `Input-Position-at-Last-Final`

每进入一个终态就更新这两个变量。
当走到死状态时，就知道：

- 最近一次成功匹配是什么 token
- 它在什么位置结束


例如：

- 识别出 `IF`
- 遇到空白时跳过并继续
- 某些非法字符（如 `-` 在不合法位置）会报错然后继续
- 说明自动机并不是只识别一个 token，而是不断：

    1. 从当前位置开始

    2. 找最长匹配
  
    3. 输出一个 token 或执行忽略动作
  
    4. 从下一个位置继续

## 2.4. NFA

- NFA
- Thompson 构造
- 子集构造
- DFA 最小化


### 2.4.1.定义NFA（非确定有限自动机）

- 同一状态在同一输入符号下，可以去多个不同状态
- 还允许 **ε-边**
- ε-边不消耗输入字符

这是 NFA 和 DFA 的本质区别。


- 有 ε-边时，即使当前有正常字符边，也可以先走 ε-边
- ε-边不消耗字符

这让 NFA 更灵活，也更容易构造。


定义 NFA 如何接受字符串：

> 只要存在**某一条可能路径**最终到达终态，NFA 就接受。

和 DFA 的区别是：

- DFA：唯一运行路径
- NFA：只要有一条成功路径就行


### 2.4.2. Thompson 构造

#### 2.4.2.1. 意义

> RE 到 NFA 很容易，这就是 Thompson 构造的价值

如果我们知道：

  1. 基本正则如何转成基本 NFA
  
  2. 正则的三种构造操作如何在 NFA 上模拟

那么任意正则都能转成 NFA。

#### 2.4.2.2. 流程

每个正则 `M` 都会对应一个带 tail（起始边） head（终止状态）的 NFA 片段。



这是最关键的 **Thompson 构造规则图**。它给出：

- `a` 如何转 NFA
- `ε` 如何转 NFA
- `M|N` 如何转 NFA
- `M·N` 如何转 NFA
- `M*` 如何转 NFA
- `M+` 视作 `M·M*`
- `M?` 视作 `M|ε`
- `[abc]`
- `"abc"`


- **并**：新建开始和结束，用 ε 分叉/汇合
- **连接**：把前一个的尾和后一个的头接起来
- **闭包**：允许回环和跳过


### 2.4.3. 子集构造，NFA 转 DFA

> DFA 的一个状态，其实可以看成 **NFA 状态集合**。

例子中：

- 先算 `closure({1}) = {1,4,9,14}`
- 读入 `i` 后得到新的状态集合
- 再读 `n` 后得到新的状态集合
- 最终接受


- 从 `d1 = closure({s1})` 开始
- 对每个状态集合 `d` 和每个输入符号 `c`
- 计算 `d' = DFAedge(d,c)`
- 不断重复，直到没有新状态集合出现

这就是子集构造的核心过程。

------

## 第17页

解释为什么这样构造出的机器就是 DFA：

- 状态：所有可达的 NFA 状态集合
- 转移：`DFAedge(d,c)`
- 字母表不变
- 初态：`d1`
- 终态：包含某个 NFA 终态的那些集合

还提到：

- 若 NFA 有 `n` 个状态，理论上最多会有 `2^n` 个集合状态

这页是“为什么子集构造成立”的总结。

------

## 第18页

正式定义 **ε-closure**：

- 对一个状态集合 `S`
- `closure(S)` 是只走 ε-边、不消耗输入所能到达的全部状态集合

并定义：

- `edge(s,c)`：从状态 `s` 走一条标记为 `c` 的边能到达的状态集合

这是后面公式的基础。

------

## 第19页

讲如何迭代计算 `closure(S)`，并说明它一定终止：

- 因为状态集合只能变大
- NFA 状态总数有限

这页解决的是“算法能不能停”。

------

## 第20页

定义最重要的函数：

$DFAedge(d,c)=closure\left(\bigcup_{s\in d} edge(s,c)\right)$

直观理解：

1. 从 `d` 中每个状态出发
2. 走一条 `c` 边
3. 把所有结果合并
4. 再把这些状态的 ε-closure 全部补齐

这就是读一个字符后 DFA 新状态怎么来。

------

## 第21页到第45页（子集构造算法详细例子）

这一大组页在逐步演示算法如何执行。虽然页数多，但逻辑很统一。我按页概括：

### 第21页

给出算法伪代码、NFA 图、`states` 表和 `trans` 表的初始空框架。

### 第22页

初始化：

- `states[0] = {}`
- `states[1] = closure({s1}) = {s1,s2,s6}`
  这里把空集也作为死状态。

### 第23页

开始处理 `states[0]` 在输入 `a` 下的转移。

### 第24页

得到 `DFAedge({},a) = {}`。
也就是说死状态读 `a` 仍然留在死状态。

### 第25页

把这条转移填入表中：`0 --a--> 0`。

### 第26页

继续处理 `states[0]` 在 `b` 下的转移。

### 第27页

得到 `DFAedge({},b) = {}`。

### 第28页

填表：`0 --b--> 0`。
到这里死状态完整了。

### 第29页

开始处理 `states[1] = {s1,s2,s6}`。

### 第30页

先看输入 `a`。

### 第31页

得到：

- `DFAedge({s1,s2,s6}, a) = {s3,s4,s7,s8}`
  这是一个新状态集合。

### 第32页

把这个新集合记成 `states[2] = {s3,s4,s7,s8}`，并填上 `1 --a--> 2`。

### 第33页

继续处理 `states[1]` 在 `b` 下的转移。

### 第34页

得到 `DFAedge({s1,s2,s6}, b) = {}`。

### 第35页

填表：`1 --b--> 0`。

### 第36页

开始处理 `states[2] = {s3,s4,s7,s8}`。

### 第37页

先看输入 `a`。

### 第38页

得到 `DFAedge({s3,s4,s7,s8}, a) = {}`。

### 第39页

填表：`2 --a--> 0`。

### 第40页

继续看 `states[2]` 在 `b` 下。

### 第41页

得到：

- `DFAedge({s3,s4,s7,s8}, b) = {s5,s8}`
  这是又一个新状态集合。

### 第42页

记为 `states[3] = {s5,s8}`，并填表：`2 --b--> 3`。

### 第43页

开始处理 `states[3] = {s5,s8}`。

### 第44页

看输入 `a`。

### 第45页

最终得到：

- `3 --a--> 0`
- `3 --b--> 0`

并画出最终 DFA。

------

### 这一整组例子的最终结果

最后得到的 DFA 状态及转移是：

- `0 = {}`
- `1 = {s1,s2,s6}`
- `2 = {s3,s4,s7,s8}`
- `3 = {s5,s8}`

转移表：

- `0 --a--> 0`
- `0 --b--> 0`
- `1 --a--> 2`
- `1 --b--> 0`
- `2 --a--> 0`
- `2 --b--> 3`
- `3 --a--> 0`
- `3 --b--> 0`

这就是“把 NFA 机械地变成 DFA”的完整例子。

------

## 第46页

对子集构造做总结：

- 算法只访问**可达**状态，不会真的生成全部 `2^n` 个状态
- 这很重要，否则转移表会爆炸
- DFA 中一个状态若包含某个 NFA 终态，则它是 DFA 终态
- 但仅知道“是终态”还不够，还要知道“识别的是哪种 token”
- 如果一个集合中有多个 NFA 终态，要按**规则优先级**决定 token
- 构造完后：
  - `states` 数组可以丢掉
  - 真正做词法分析只需要 `trans` 矩阵

这页非常重要，因为它把理论算法和工程实现接起来了。

------

## 第47页

过渡页，进入 **DFA Minimization**。

------

## 第48页

重新展示之前的 NFA（IF、ID、NUM、error 的合并图），作为最小化的起点。

------

## 第49页

说对子集构造得到的 DFA：

- 它是正确的
- 但**不是最小的**
- 也就是状态数还可以减少

这页告诉你：

> 子集构造得到的是“能用的 DFA”，不一定是“最省的 DFA”。

------

## 第50页

定义 **等价状态**：

两个状态 `s1` 和 `s2` 等价，当且仅当：

- 从 `s1` 出发接受某串 `σ`
- 当且仅当从 `s2` 出发也接受 `σ`

课件举了 Figure 2.8 中的两个例子：

- `{5,6,8,15}` 和 `{6,7,8}`
- `{10,11,13,15}` 和 `{11,12,13}`

它们可合并。

这是最小化的本质：

> 找出“行为完全一样”的状态，合并它们。

------

## 第51页

说明一个“看起来合理但不充分”的判定条件：

- 同时都是终态或同时都不是终态
- 且每个输入字符转移到的目标一样

这个条件**不够一般**。
课件举例说明：

- 某些状态虽然目标状态名字不同
- 但这些目标状态本身是等价的
- 所以原状态仍可能等价

也就是说：

> 判断等价不能只看一步，要看“未来所有行为”。

------

## 第52页

引入 **distinguishable states（可区分状态）**：

若存在字符串 `x`，使得：

- 从 `s` 出发读 `x` 后到终态
- 从 `t` 出发读 `x` 后不到终态

或反过来，

那 `x` 就区分了 `s` 和 `t`。

所以：

- 能被某个串区分开的状态，不等价
- 不能被任何串区分开的状态，才等价

这是最小化的逻辑基础。

------

## 第53页

给出最小化算法的思想版：

- 先把显然不同的状态分开
- 再不断细分分组
- 直到不能再分

具体思想：

1. 终态和非终态先分开
2. 若两个状态经过某输入后会落入不同组，它们也应分开
3. 最后每组中的状态互相等价

这就是经典的**划分细化**思想。

------

## 第54页

给出正式算法：

1. 初始划分 `Π = {S-F, F}`
2. 对 `Π` 中每个组 `G`：
   - 若组内状态在某些输入字符下转移到不同组
   - 就把 `G` 再细分
3. 如果新划分和旧划分一样，结束；否则继续

这是最小化的标准 partition-refinement 算法。

------

## 第55页

说明划分结束后如何重建最小 DFA：

- 每个最终分组挑一个代表状态
- 最小 DFA 的状态就是这些代表
- 原初态所属组的代表成为新初态
- 含终态的组代表成为新终态
- 转移按“代表的目标组代表”来连

这页就是“分完组以后怎么造新图”。

------

## 第56页到第72页（DFA 最小化详细例子）

这一组页在一步一步跑一个五状态例子 `A,B,C,D,E`。我按页总结：

### 第56页

初始化划分：

- `{{A,B,C,D}, {E}}`
  因为 `E` 是终态，其余不是。

### 第57页

检查组 `{A,B,C,D}` 在输入 `a` 下的行为。

### 第58页

发现没有必要分裂，`a` 下 **No action**。

### 第59页

检查 `{A,B,C,D}` 在输入 `b` 下。

### 第60页

发现要分裂：

- `{A,B,C,D} => {A,B,C}, {D}`
  说明 `D` 在 `b` 下行为不同。

### 第61页

更新划分为：

- `{{A,B,C}, {D}, {E}}`

### 第62页

开始检查组 `{E}`。

### 第63页

`{E}` 在 `a/b` 下都不引发新的分裂，No action。

### 第64页

开始下一轮，当前划分仍是：

- `{{A,B,C}, {D}, {E}}`

### 第65页

检查 `{A,B,C}` 在 `a` 下。

### 第66页

`a` 下无动作，不分裂。

### 第67页

检查 `{A,B,C}` 在 `b` 下。

### 第68页

发现再次分裂：

- `{A,B,C} => {A,C}, {B}`

### 第69页

更新划分为：

- `{{A,C}, {B}, {D}, {E}}`

### 第70页

继续检查：

- `{D}` 和 `{E}` 在 `a/b` 下都不再导致分裂
- `{A,C}` 在 `a/b` 下也不再导致分裂
  所以划分稳定。

### 第71页

选择代表状态 `A,B,D,E`，得到最小 DFA 的转移表：

- `A --a--> B`
- `A --b--> A`
- `B --a--> B`
- `B --b--> D`
- `D --a--> B`
- `D --b--> E`
- `E --a--> B`
- `E --b--> A`

### 第72页

用分组名字写得更准确：

- `{A,C} --a--> {B}`
- `{A,C} --b--> {A,C}`
- `{B} --a--> {B}`
- `{B} --b--> {D}`
- `{D} --a--> {B}`
- `{D} --b--> {E}`
- `{E} --a--> {B}`
- `{E} --b--> {A,C}`

这一页是最终答案页。

------

## 第73页

讲一个**词法分析中特别重要的修正**：

普通 DFA 最小化里，所有终态可以先放在一个组里。
但**词法分析器里不行**，因为不同终态可能对应不同 token 类型。

例如：

- 一个终态表示 `IF`
- 另一个终态表示 `ID`

它们即使“结构上像”，也不能合并。

所以词法分析器的初始划分不是：

- `{S-F, F}`

而应是：

- `{S-F, F1, F2, ..., Fk}`

其中 `Fi` 是识别同一种 token 的终态集合。

这页非常重要，属于“编译器版本的 DFA 最小化”。

------

## 第74页

进入 **2.5 Lex: A Lexical Analyzer Generator**。标题页。

------

## 第75页

路线图页，表示现在已经走完整条链：

- 词法规则
- 正则
- NFA
- DFA
- 最小化
- 现在要落到工具：Lex

------

## 第76页

说明为什么会有 Lex：

- DFA 构造是机械劳动
- 适合让计算机自动完成
- 所以有 lexical-analyzer generator
- Lex 能把正则规格说明自动翻译成 DFA，并生成 C 程序

这一页的意思是：

> 既然理论链条已经打通，就不该手工重复造轮子。

------

## 第77页

说明 Lex 生成的结果：

- 会产生一个 C 函数 `yylex`
- 它就像 `getToken`
- 本质是一个**表驱动的 DFA 实现**
- 最流行版本是 **flex**

也就是说，前面你学的转移矩阵和动作数组，在 Lex/flex 里会自动生成。

------

## 第78页

给出 Lex 输入文件 `a.l` 的三段结构：

```lex
{ definitions }
%%
{ rules }
%%
{ auxiliary routines }
```

并给了示例：

- `%{ ... %}` 放 C 代码
- `digit [0-9]`
- `number {digit}+`
- 规则中把十进制转成十六进制输出
- `main()` 调用 `yylex()`

这一页很关键，因为它把“理论规则”变成了“工具输入格式”。

------

## 第79页

解释第一部分 **definitions**：

- 在第一个 `%%` 之前
- `%{...%}` 中可写 C 头文件、全局变量等
- 后面可写正则缩写、状态声明

例如：

- `digit [0-9]`
- `number {digit}+`

这相当于“预备定义区”。

------

## 第80页

解释第二部分 **rules**：

- 每条规则由：
  - 一个正则表达式
  - 一个动作（C 代码片段）
- 当正则匹配时执行对应动作
- `yytext`：当前匹配到的字符串
- `yyleng`：匹配长度

这是 Lex 最核心的使用方式。

------

## 第81页

解释第三部分 **auxiliary routines**：

- 放辅助函数
- 比如 `main()`
- 或规则里调用但别处没定义的函数

也就是说，这是普通 C 代码补充区。

------

## 第82页

完整重复了一遍示例 Lex 程序。
这个程序的作用是：

- 识别十进制数字
- 把它们转为十六进制输出
- 统计大于 9 的替换次数
- 最后打印统计结果

这是一个最小可运行的 Lex 示例。

------

## 第83页

练习与实验提醒：

- Exercise: 2.1, 2.2, 2.5(a,b), 2.6
- lab0 本周日截止
- lab1 词法分析部分尽快开始

这页不是理论内容，但说明这一章是会落到实验实现上的。

------

## 第84页

结束页。

------

# 四、把整章压成一条“主线逻辑”

如果你已经按页看完，下面这一段是帮助你形成整体框架的。

------

## 1. 词法分析到底干什么

输入：

- 一串字符

输出：

- 一串 token

忽略：

- 空白
- 注释
- 某些非 token 内容

并且要处理：

- 非法字符报错
- 关键字与标识符区分
- 最长匹配问题

------

## 2. token 怎么描述

先用自然语言描述规则，例如：

- 标识符第一个字符必须是字母
- 后面可跟字母数字
- `_` 算字母
- 大小写敏感

但自然语言不适合直接实现，所以要用正则表达式。

------

## 3. 正则表达式解决什么问题

正则表达式负责：

> 用有限形式，精确描述某类字符串集合。

例如：

- `[a-z][a-z0-9]*` 描述标识符
- `[0-9]+` 描述整数
- `if` 描述关键字
- 注释/空白也能描述

但正则本身只是“规则”，不是高效执行程序。

------

## 4. 为什么要引入自动机

因为自动机适合逐字符扫描输入。
特别是 DFA：

- 每一步唯一
- 容易做成表驱动程序
- 很适合实现 lexer

------

## 5. 为什么又要引入 NFA

因为：

- 正则 → NFA 很容易
- NFA 允许 ε-边和非确定性
- Thompson 构造可以机械地把正则翻成 NFA

所以 NFA 是“从正则到 DFA”的中间桥梁。

------

## 6. NFA 怎么变成 DFA

用 **子集构造**：

- DFA 的一个状态 = 一组 NFA 状态
- 读字符时，把所有可能状态一起推进
- 再补上 ε-closure

所以本质上不是“猜路径”，而是“并行考虑所有路径”。

------

## 7. 为什么还要最小化 DFA

因为子集构造得到的 DFA 可能状态冗余。
最小化的目标是：

- 合并行为完全相同的状态
- 减少状态数
- 压缩转移表

但在词法分析器中：

- 不同 token 对应的终态不能乱合并

所以最小化必须考虑 token 类型。

------

## 8. Lex 做了什么

Lex/flex 自动把这些步骤做完：

- 读入正则规格说明
- 自动构造 DFA
- 自动生成 `yylex()`
- 最终得到真正可运行的词法分析器

所以现实里我们通常：

- 不手写完整 DFA
- 而是写 Lex 规则，让工具生成。

------

# 五、初学者最容易混淆的点

------

## 1. token 不是字符

字符是最原始输入。
token 是**分类之后的词法单元**。

例如：

- 字符串 `"if8"` 是字符序列
- 它可能被识别为一个 `ID` token，而不是 `IF` 和 `NUM` 两个 token

------

## 2. 正则不是执行器，是描述器

正则表达式负责说明“什么样的串合法”。
真正执行匹配的，是自动机。

------

## 3. DFA 和 NFA 接受的语言能力一样

NFA 不是“更强”，只是“更容易构造”。
DFA 不是“更会表达”，只是“更适合执行”。

------

## 4. 最长匹配和规则优先级不是一回事

- **最长匹配**：先比较谁吃得更多
- **规则优先级**：在同样长的前提下，看谁写得更靠前

先最长，再优先级。顺序不能反。

------

## 5. 词法分析器中的终态不能随便合并

普通最小化把所有终态先放一起。
但 lexer 中：

- `IF` 的终态
- `ID` 的终态

不能视为等价。

------

# 六、这一章你至少要记住的结论

1. **词法分析 = 字符流 → token 流**。
2. **正则表达式**适合描述 token。
3. **DFA**适合实现词法分析器。
4. **NFA**是正则到 DFA 的中间桥梁。
5. **Thompson 构造**：RE → NFA。
6. **子集构造**：NFA → DFA。
7. **划分细化**：DFA → 最小 DFA。
8. 词法分析中要遵循 **最长匹配 + 规则优先级**。
9. 现实中可用 **Lex/flex** 自动生成 lexer。

------

# 七、适合考前背诵的超短总结

```markdown
- 词法分析器把字符流切成 token 流。
- token 可用正则表达式描述。
- 正则表达式先转 NFA（Thompson 构造）。
- NFA 再转 DFA（子集构造）。
- DFA 可进一步最小化（划分细化）。
- 词法分析器实现时要处理最长匹配和规则优先级。
- 不同 token 的终态在最小化时不能混为一组。
- Lex/flex 可以根据词法规则自动生成 yylex()。
```

------

如果你要，我下一条可以继续把这份笔记**整理成更适合背诵的“精简版期末复习提纲”**。